---
format_version: 10
pipelines:
  java_microservice:
    group: microservices
    lock_behavior: unlockWhenFinished
    label_template: "${git_java_microservice}"
    materials:
      git_java_microservice:
        git: https://github.com/kfreimanistdl/api_tests_ci.git
        branch: main
        shallow_clone: true
        auto_update: true
    stages:
      - build: 
          jobs: 
            build: 
              tasks: 
                - exec:
                    command: bash
                    arguments: 
                      - -c
                      - echo "Building java app"
      - deploy-dev: 
          jobs: 
            deploy: 
              tasks: 
                - exec:
                    command: bash
                    arguments: 
                      - -c
                      - echo "Deploy to DEV"
      - test-dev: 
          jobs: 
            test: 
              tasks: 
                - exec:
                    command: bash
                    arguments: 
                      - -c
                      - echo "Run tests on DEV"
      - deploy-int: 
          jobs: 
            delpoy: 
              tasks: 
                - exec:
                    command: bash
                    arguments: 
                      - -c
                      - echo "Deploy to INT"
      - test-int: 
          jobs: 
            test: 
              tasks: 
                - exec:
                    command: bash
                    arguments: 
                      - -c
                      - echo "Run tests on INT"
      - deploy-prod: 
          jobs: 
            deploy: 
              tasks: 
                - exec:
                    command: bash
                    arguments: 
                      - -c
                      - echo "Deploy to PROD"
      - test-prod: 
          jobs: 
            test: 
              tasks: 
                - exec:
                    command: bash
                    arguments: 
                      - -c
                      - echo "Run tests on PROD"